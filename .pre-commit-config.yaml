repos:
  - repo: local
    hooks:
      - id: janus-validate
        name: Janus validate backlog / devlog
        entry: janus validate
        language: system
        files: ^docs/(backlog|devlog)\.md$
        pass_filenames: true
